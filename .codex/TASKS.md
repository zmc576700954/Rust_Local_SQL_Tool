# 项目主任务清单

## 当前任务

### 2026-05-07｜成品化方案设计与第一批性能落地

- 状态：进行中（方案已完成；第一批性能/基线修复已落地）
- 摘要：已完成成品化路线设计，并持续落地高频可用性修正：恢复 Rust/前端测试与构建基线、加入按 `db_id` 的连接复用缓存、把 schema 拉取改为批量查询并加缓存、让表首屏不再阻塞 `COUNT(*)`、写操作后清理 metadata cache，并补上当前一轮 AI 杂音收敛、`/chat` 历史透传、历史复用与库表浏览便捷化。
- 明细：`/docs/tasks/productization-shipready-tasklist.md`

## 最近已完成任务摘要

- 暂无更早摘要记录。

## 归档索引

- 暂无归档。


- Update: landed the next desktop-performance step with real Tauri IPC for read-only workbench execution, table paging, and table schema fetch; unsupported DB types or ssh/ssl profiles keep HTTP fallback. Validation stayed green (`cargo check` for `web-ui/src-tauri`, `cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: landed chunked workbench preview for read-only queries across both HTTP and Tauri paths. Initial preview now returns 200 rows with load-more metadata instead of always materializing the full 1000-row preview cap up front.

- Update: optimized MySQL row serialization on the hottest query/table-preview paths by precomputing per-column decode strategy instead of repeating multi-branch `try_get` fallback chains for each cell.

- Update: implemented real MySQL query cancel for the current execute/workbench hot path. Web `/backend/execute` and desktop `workbench_run` now accept a client-generated `cancel_token`, register the dedicated MySQL session id, and cancel with `KILL QUERY` via `/backend/execute/cancel` or Tauri `workbench_cancel`. Frontend now exposes a live `Cancel` button and canceled runs map to `ERR_CANCELED` instead of a generic error. Validation stayed green (`cargo test --workspace`, `cargo check` for `web-ui/src-tauri`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: landed the next table-browse performance step on the current UI hot path. `TableDataView` now detects a single-column stable key (PRIMARY first, then NOT NULL unique) and uses keyset-style next/prev paging for the default browse flow, while preserving offset fallback for custom non-key sorts or tables without a stable single-column key. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: split DB test into lightweight Stage A and optional Stage B capability probing. `/backend/db/test` now defaults to handshake/auth/SSL/SSH/ping only, onboarding explicitly opts into metadata probing for database list bootstrap, and connection retest in the sidebar stays fast by default. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: added the first minimal perf probe baseline for productization. Shared perf summary types now exist in `core_lib`, backend exposes `POST /backend/diagnostics/perf/probe`, and desktop exposes Tauri `perf_probe` for `connect_warm` plus `query_select_small`, both with built-in p50/p95 budgets. Validation stayed green (`cargo test --workspace`, `cargo check` for `web-ui/src-tauri`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: extended perf probes to cover `catalog_first_paint` and `table_first_page` on both backend and desktop direct-path runtimes. The new table probe includes metadata fetch + first-page row serialization timing, and the catalog probe tracks the current schema-build first paint path with metadata cache reset per iteration. Validation stayed green (`cargo test --workspace`, `cargo check` for `web-ui/src-tauri`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: added a minimal `Perf Diagnostics` front-end entry in `ToolsNav` and a new `PerfDiagnosticsPanel` tab so probes can be run interactively from the product UI. The panel supports connection selection, iteration control, read-only SQL input, table selection, and displays p50/p95 plus raw samples for all four current probe operations. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: enhanced `PerfDiagnosticsPanel` with budget comparison highlighting for p50/p95 and a direct `Export JSON` action so probe runs can be archived and compared outside the UI. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: used parallel sub-agent analysis to split current function blocks, then landed a focused prompt/runtime follow-up: shared structured SQL prompt builder, `/chat` schema-cache reuse on the NL->SQL hot path, and instant table page/page-size navigation without debounce. Details: `/docs/tasks/productization-shipready-tasklist.md`.
- Update: added local Perf Diagnostics history plus same-operation baseline comparison. Probe runs are now persisted in localStorage, can be reloaded from a Recent Runs list, and expose direct p50/p95/avg/max delta cards for before/after optimization comparison. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: expanded Perf Diagnostics to cover `connect_cold`, `query_write_small`, `explain_plan`, and `cancel_latency` on both backend and desktop direct paths. The write probe now uses a session-local temporary table, explain probe auto-wraps read-only SQL with `EXPLAIN`, and cancel probe measures real `KILL QUERY` completion latency. Validation stayed green (`cargo test --workspace`, `cargo check` for `web-ui/src-tauri`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).

- Update: landed the next minimal structure split after the prompt/runtime follow-up. `web-ui/src/queryAiActions.ts` now holds the query/AI action path extracted from `App.tsx`, and `web-server/src/ai_handlers.rs` now owns `/api/ai/*` plus AI knowledge handlers extracted from `main.rs`, with routes kept stable. Details: `/docs/tasks/productization-shipready-tasklist.md`.
- Update: added one-click full perf suite execution in `PerfDiagnosticsPanel`, including ordered multi-probe runs, live suite progress, unified suite summary/export, and automatic per-operation history capture for later comparisons. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added persistent full-suite archive plus suite-to-suite comparison in `PerfDiagnosticsPanel`, including suite selection, baseline selection, per-operation p50/p95/avg deltas, and selected-suite export. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: extracted the Query tab editor/action area from `web-ui/src/App.tsx` into `web-ui/src/components/QueryEditorActionPanel.tsx`, keeping App as the state/effect owner while shrinking the main render block. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: extended `PerfDiagnosticsPanel` full-suite snapshots with environment/notes metadata plus compare filters (`same connection only`, `same build version only`) so RC/nightly and before/after optimization baselines are easier to keep separate. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added backend full-suite archive endpoints (`/backend/diagnostics/perf/suites`) and switched `PerfDiagnosticsPanel` to prefer backend suite persistence with localStorage fallback, so perf baselines are now retained server-side instead of only in the browser. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added backend suite detail lookup plus pinned baseline endpoints (`/backend/diagnostics/perf/suites/:suite_id`, `/baseline`) and wired `PerfDiagnosticsPanel` to pin a selected suite as the default compare baseline, preferring that locked baseline whenever filters allow it. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added `Export Diff JSON` plus suite-level Perf Gate cards in `PerfDiagnosticsPanel`, so each full-suite run now surfaces a direct PASS/WARN/FAIL release signal based on budget failures and pinned-baseline regressions. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: strengthened the shared SQL prompt path in `core_lib/src/ai/prompting.rs`, plus matching guidance in `core_lib/src/ai/planner.rs` and `core_lib/src/ai_agent.rs`, so SQL generation/fix/explain/optimize flows now emphasize schema-grounded joins, ambiguity reporting, and intent-preserving edits. Validation stayed green (`cargo test --workspace`).
- Update: added weak-signal / ambiguity diagnostics to `core_lib/src/ai/prompting.rs` relevant-table selection, plus low-risk singular-plural and `snake_case` token grounding, so the shared SQL prompt can better detect fallback schema context without changing route contracts. Scoped validation stayed green (`cargo test -p core_lib`); full workspace test is currently blocked by an unrelated `web-server/src/main.rs` recursion-limit failure.
- Update: expanded the shared SQL prompt output schema in `core_lib/src/ai/prompting.rs` with `task_type`, `sql_empty_reason`, `missing_information`, and `grounding_evidence`, while keeping the existing extractor contract stable via a compatibility test in `core_lib/src/ai/extractor.rs`. Scoped validation stayed green (`cargo test -p core_lib`).
- Update: unblocked `cargo test --workspace` by adding `#![recursion_limit = "256"]` to `web-server/src/main.rs`, which fixes the unrelated large `serde_json::json!` test-payload compilation limit without changing runtime behavior. Workspace validation is green again (`cargo test --workspace`).
- Update: finished the next AI-side passthrough plus `/chat` structure split in one pass. `core_lib/src/ai/extractor.rs` + `planner.rs` now carry `task_type` / `sql_empty_reason` / `missing_information` through all SQL-generation paths, and `web-server/src/ai_handlers.rs` now owns `/chat` while `/chat` and `/api/ai/query` both surface/log the new metadata. Validation stayed green (`cargo test --workspace`).
- Update: added backend persistence for suite diff/gate reports via `/backend/diagnostics/perf/suite-diffs`, and wired `PerfDiagnosticsPanel` to archive and restore compare reports by current/baseline suite pair while surfacing the archived diff path in the UI. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added archived diff history navigation in `PerfDiagnosticsPanel`, so backend diff archives for the selected suite are now listed in the compare card and can restore the archived current/baseline pair back into the UI with one click. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added `All` / `PASS` / `WARN` / `FAIL` quick filters for archived diff history in `PerfDiagnosticsPanel`, including filtered/total counts and a filter-aware empty state for faster suite compare triage. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: used parallel sub-agents to land the current DB+AI usability step. The AI path now sends explicit `mode/current_sql`, `/chat` actually forwards `chat_history`, extractor fallback no longer promotes chatter into SQL, query tabs keep `executionDbId` for cancel/load-more, SQL history refreshes immediately with one-click `Open`, and DB Explorer now auto-expands connections plus filters/opens tables and views. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added default archived-diff sorting (`FAIL` -> `WARN` -> `PASS`, newest first within each status) plus a `Pinned only` filter in `PerfDiagnosticsPanel` to speed up locked-baseline release-gate review. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added keyword search for archived diff history in `PerfDiagnosticsPanel`, matching baseline label, current/baseline suite id, and archive path while composing cleanly with the existing status and pinned-only filters. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added a one-click `Reset filters` action for archived diff history in `PerfDiagnosticsPanel`, resetting status, pinned-only, and search-query filters back to defaults and auto-disabling when nothing is active. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added a `Current baseline only` filter for archived diff history in `PerfDiagnosticsPanel`, limiting rows to the active compare baseline and auto-resetting when no compare baseline is selected. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added `Copy IDs` and `Copy path` shortcuts for each archived diff row in `PerfDiagnosticsPanel`, while keeping the existing row-click restore behavior unchanged. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: added `Export filtered JSON` for archived diff history in `PerfDiagnosticsPanel`, exporting the current suite/baseline context, active filters, and the currently filtered archived diff rows. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: batched the remaining archived-diff history UX refinements in `PerfDiagnosticsPanel`: details expand/collapse, active-filter tags, per-row item export, full current/baseline/archive-path detail display, and field-level copy actions, while keeping row-click restore behavior unchanged. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: switched to the next front-end mainline and batched a focused table/query-results usability pass. `TableDataView` now exposes active filter/sort chips plus one-click reset, `DataTable` adds clear-filters/sorts plus row JSON/SQL copy actions, and `QueryResultsPanel` adds loaded-result JSON copy/export with clearer chunk/preview metadata. Validation stayed green (`npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: landed the next table-browse flexibility batch. Column filters now support `not equals`, `starts/ends with`, and `is null / is not null`, the table footer exposes removable active-filter chips, and `/backend/table_data` now understands the same operators so browse filtering stays consistent end to end. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: finished the remaining current browse/result-usability subitems in one batch. Table filters now support `between` plus `in / not in`, query-result tables gained row JSON/SQL copy plus large-value preview, and the table-data backend now understands the same richer operators end to end. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).
- Update: switched to the next Query Workbench mainline and landed a batched history/explain upgrade. SQL history now supports search, status/current-db filtering, copy + rerun actions, and richer persisted execution metadata; the Explain tab now supports visual/table switching, refresh, and SQL copy. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui`, `npm run build --prefix web-ui`).


- Update: finished the current Query Workbench multi-statement batch on the existing front-end execution path. `App.tsx` now executes split SQL statements sequentially and tracks per-statement result state, `QueryResultsPanel.tsx` now exposes statement selectors plus inline per-statement error/status rendering, and `sqlStatements.ts` adds the shared splitter/statement metadata helpers. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next Query Workbench mainline and landed manual transaction mode in one batch. Query tabs now track `auto/manual` transaction state with commit/rollback controls, `/execute` now supports pinned transaction sessions plus `/execute/transaction`, and the Tauri direct-path workbench mirrors the same transaction flow for desktop usage. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`, `cargo test --workspace`, `cargo check` in `web-ui/src-tauri`).

- Update: switched to the next Query Workbench mainline and landed a batched snippets/bookmarks quick-access pass. Query tabs now support local bookmark save/open/run, the command palette now searches smart snippets plus saved bookmarks, and the action bar exposes `Save Bookmark` in both full and compact layouts. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next Query Workbench mainline and landed a batched error explain + AI repair pass. Query results now show failed SQL context plus `Explain Error` / `Fix with AI`, while query tabs track reusable AI error insight with suggested-SQL apply/copy actions. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next Query Workbench mainline and landed a batched session-info/server-variables pass. The query toolbar now opens a dedicated Session Info tab, backend exposes `/backend/sql/session-info`, and the new panel supports refresh plus structured summary/session/global variable inspection. Validation stayed green (`cargo test --workspace`, `npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next Query Workbench mainline and landed a batched result-compare/export refinement pass. Query tabs now support saved baseline snapshots plus compare JSON copy/export, and the results panel surfaces baseline controls with added/removed/unchanged summary counts. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-grid/results mainline and landed a batched large-value viewer/editor polish pass. Query results and table grids now preview structured cells inline, large-value modals support copy/download, editable grid cells can be updated through a full-screen viewer/editor, and structured result exports now serialize object columns as JSON. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-grid personalization mainline and landed a batched column-layout pass. Result grids and table grids now support persisted column visibility, left/right order adjustment, and header drag-resize width memory, all kept in local browser storage. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched duplicate-row / NULL-default quick-action pass. The editable grid context menu now supports duplicating rows, clearing generated/primary keys on clone, setting nullable cells to NULL, and applying literal schema defaults when they can be materialized safely on the client. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched clipboard/manual row-paste import pass. The editable grid now supports modal-based TSV/JSON paste import, clipboard read shortcut, schema-aware row coercion, and draft-row append without touching the database until Save. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched save-review pass before persistence. The editable grid now opens a pre-commit review modal from both the dirty banner and global save shortcut, previews inserted/updated/deleted changes before write, drops reverted row edits from dirty state, and keeps local drafts intact if a save fails. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched row-level validation pass before save. The editable grid now highlights invalid cells inline, blocks confirm-save until numeric/boolean/JSON/max-length issues are fixed, exposes jump-to-error summaries in both the dirty banner and save-review modal, and strips blank auto-generated insert columns before write. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched save-failure detail retention pass. The editable grid now records per-row save failures inside the review modal and dirty banner, removes successful operations from pending local draft state while keeping only failed items for retry, and reindexes failed draft inserts so jump-to-error still points at the correct remaining row. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched save-error semantic mapping pass. The editable grid now classifies save failures into stale-row / unique / required / foreign-key / length / invalid-value / read-only buckets, surfaces friendly guidance plus raw backend text in the save-review flow, and treats update/delete `affected_rows = 0` as real stale-row failures instead of silent success. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched stale-row recovery action pass. The editable grid now lets users drop one failed change, drop all failed changes, drop all stale-row conflicts, or reload the latest server copy with pending edits cleared, while keeping failure-report insert indices aligned after draft removals. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next data-editing mainline and landed a batched stale-row refresh/rebase recovery pass. The editable grid now retains stale-row recovery context, refreshes in place without unmounting the grid, supports one-click reapply/redelete recovery for single or all stale conflicts, and rebases recovered edits onto the refreshed server row so unrelated server-side changes are preserved. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next data-editing mainline and landed a batched stale-row conflict diff/resolution review pass. The editable grid now opens a dedicated stale-conflict modal with original/pending/server-side value comparison, per-column `Use Mine` / `Use Server` selection for update conflicts, freshness warnings tied to the table data revision, and in-place rebase/redelete actions once the refreshed row is visible. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next data-editing mainline and landed a batched stale-conflict queue/recommended-merge pass. The conflict modal now supports prev/next queue navigation, one-click recommended/mine/server presets, `Apply ... & Next` / `Use Latest & Next` continuous handling, and recommended merge counts for faster multi-conflict cleanup. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next data-editing mainline and landed a batched stale-conflict overview/risk-triage pass. The editable grid now exposes a dedicated conflict queue modal with one-click high-risk/needs-refresh/safe-edit filtering, queue-level summary counts, row-level quick review/apply actions, and safe batch recommended-merge apply for visible update conflicts. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
- Update: switched to the next data-editing mainline and landed a batched stale-conflict overview search/sort + bulk latest/refresh pass. The conflict queue now supports free-text search, risk/conflict/row/action sorting, batch `Use Latest for Visible`, batch `Refresh Visible Needs Refresh`, and row-level `Use Latest` from the overview itself. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched stale-conflict grouped-overview + safe keep-mine pass. The conflict queue now renders collapsible risk groups with high-risk-first default focus, filter changes re-focus the grouped view, and safe stale edits support batch/row-level `Keep Mine` alongside the existing recommended/latest actions. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).

- Update: switched to the next data-editing mainline and landed a batched scoped stale-conflict queue-review pass. Review now preserves visible/filter/group scope from the overview, group headers expose direct review/refresh actions, scoped prev/next and `... & Next` stay inside the chosen queue, and the conflict modal now includes `Back to Queue`. Validation stayed green (`npm test --prefix web-ui -- --run`, `npm run build --prefix web-ui`).
